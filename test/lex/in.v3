// RUN: %vector -lex %s

var x: i32;

fn main(argc: i32, argv: **i8) i32 {
    var x = 0;
    var y = x + x;
    x - y;
    x * y;
    x / y;
    x ^ y;
    x & y;
    x | y;
    ~x;
    !x;
    ++x;
    x++;
    --x;
    x--;
    x <= y;
    x < y;
    x == y;
    x != x;
    x > y;
    x >= y;
    return x;
}
