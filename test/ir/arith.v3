// RUN: %vector -emit-ir %s

fn i32 square(i32 x) {
    return x * x;
}

fn i32 sum(i32 a, i32 b, i32 c) {
    return a + b + c;
}

fn i64 prec() {
    i64 x = 1 + 2 * 3;
    i64 y = 1 / 2 - 3;
    return x % y;
}
